# Plan: Replace Email with Gmail Version

## Summary
Replace the inconsistent email address `Segamat@dew.com` with the Gmail version `Segamatdew@gmail.com` to match the Contact Us page.

## Changes Required

### 1. Update `src/App.tsx:353`
- **Location**: `ContactSection()` function, `contactInfo` array
- **Current**: `{ label: "Email", values: ["Segamat@dew.com"] }`
- **Replace with**: `{ label: "Email", values: ["Segamatdew@gmail.com"] }`

## Notes
- The build artifact (`build/assets/index-CumQKePT.js`) contains the old email but will be regenerated on next build
- Only one source file needs to be updated
- After the change, both contact locations will use the same Gmail address consistently
